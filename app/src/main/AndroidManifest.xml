<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jelebuplantationsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="JPS"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RemoveUserActivity"></activity>
        <activity android:name=".DisplaySoilHumidityActivity" />
        <activity android:name=".DisplayTemperatureActivity" />
        <activity android:name=".DeleteUserActivity" />
        <activity android:name=".AddUserActivity" />
        <activity android:name=".DisplayUserActivity" />
        <activity android:name=".TemperaturePage" />
        <activity android:name=".AdminHome" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>